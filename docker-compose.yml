version: "3.9"

services:
  myapp:
    image: ar2rworld/osyne-telegram-video-downloader:releases-0.0.1
    environment:
      - BOT_TOKEN=${BOT_TOKEN}
      - ADMIN_ID=${ADMIN_ID}
      - INSTAGRAM_COOKIES_FILE=${INSTAGRAM_COOKIES_FILE}
    volumes:
      - ${ARTIFACTS_PATH}:/app/artifacts
