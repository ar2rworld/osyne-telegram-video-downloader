version: "3.9"

services:
  myapp:
    image: ar2rworld/osyne-telegram-video-downloader:releases-0.0.1
    environment:
      - BOT_TOKEN=${BOT_TOKEN}
      - ADMIN_ID=${ADMIN_ID}
      - YT_DLP_PATH=${YT_DLP_PATH}
